# fake-URL-detection
This project is a web application built using Streamlit that detects whether a URL is legitimate or fake. It analyzes various features of the given URL and calculates a legitimacy score. Based on the score, the system classifies the URL as safe or potentially malicious, helping users avoid phishing and unsafe websites.
